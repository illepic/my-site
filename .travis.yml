language: node_js
node_js: # grabs version from `.nvmrc`
git:
  depth: 1
sudo: required
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install graphicsmagick
install:
  - npm install
script:
  - npm run compile:prod
  - npm test
